#===== BOT CONFIG =====
BOT_TOKEN=Your Telegram Bot token from @BotFather
ADMINS_ID=Comma-separated list of Telegram User IDs for administrators (e.g., 123456789,987654321)

#===== WEBHOOK CONFIG =====
WEBHOOK_DOMEN=Public URL or IP for Telegram webhook requests (e.g., https://example.com)
WEBHOOK_PORT=Port for webhook server (commonly 443, 80, or 88)
WEBHOOK_PATH=Endpoint path for webhook (e.g., /webhook)

#===== WEBAPP CONFIG =====
WEBAPP_HOST=Host for internal web application (e.g., 0.0.0.0 for local)
WEBAPP_PORT=Port for internal web application (e.g., 3000)

#===== DATABASE CONFIG =====
DB_DRIVER=Database driver: asyncpg (PostgreSQL), aiosqlite (SQLite), etc.
DB_USER=Database username
DB_PASSWD=Database password
DB_HOST=Database host address (e.g., localhost or IP)
DB_PORT=Database port (e.g., 5432 for PostgreSQL)
DB_NAME=Database name

#===== REDIS CONFIG =====
REDIS_SCHEME=Connection scheme: redis, rediss (for TLS), or unix
REDIS_USER=Redis username (if applicable, Redis 6+ ACL)
REDIS_PASSWD=Redis password
REDIS_HOST=Redis host address (e.g., localhost or IP)
REDIS_PORT=Redis port (default: 6379)
REDIS_DB=Redis database number (0-15)